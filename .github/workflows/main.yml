on: 
      push:
            paths:
                  - Project1/**
name: 🚀 Deploy website on push
jobs:
  web-deploy:
    name: 🎉 Deploy
    runs-on: ubuntu-latest
    steps:
    - name: 🚚 Get latest code
      uses: actions/checkout@v3
    
    - name: 📂 Sync files
      uses: SamKirkland/FTP-Deploy-Action@v4.3.4
      with:
        server: ftp.mito-detiege-statwebbe.webhosting.be/www
        username: mito-detiege-statwebbe@mito-detiege-statwebbe
        password: ${{ secrets.FTP_PASSWORD }}
